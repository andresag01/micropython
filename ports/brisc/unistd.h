#ifndef __UNISTD_H__
#define __UNISTD_H__

/*
 * Strictly speaking, unistd.h is a POSIX header (and I think) not from the C
 * standard. But it appears to be used in MicroPython for size_t, so we provide
 * that.
 */

#include <stddef.h>
#include <stdio.h>

#endif
